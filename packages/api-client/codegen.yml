overwrite: true
schema: "./schema.graphql"
generates:
  src/types/GraphQL.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      skipTypename: true
      declarationKind: 'interface'
